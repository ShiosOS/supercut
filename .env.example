# PipiSpy ad data API. 1 credit per search result returned; responses are cached
# in data/ so re-runs never re-spend credits.
PIPISPY_API_KEY=

# OpenRouter, used for all model calls (video questionnaire + playbook prose).
OPENROUTER_API_KEY=
